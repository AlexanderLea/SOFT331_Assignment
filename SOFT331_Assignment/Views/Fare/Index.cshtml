@model IEnumerable<SOFT331_Assignment.Models.Fare>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Type.FareTypeDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TypeOfTicket.EventName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Type.FareTypeDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TypeOfTicket.EventName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FareID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FareID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FareID })
        </td>
    </tr>
}

</table>
