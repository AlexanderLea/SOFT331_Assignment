@model IEnumerable<SOFT331_Assignment.Models.Ticket>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fare.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Journey.JourneyID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Traveller.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fare.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Journey.JourneyID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Traveller.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TicketID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TicketID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TicketID })
        </td>
    </tr>
}

</table>
