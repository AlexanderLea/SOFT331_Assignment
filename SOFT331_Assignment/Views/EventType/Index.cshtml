@model IEnumerable<SOFT331_Assignment.Models.EventType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")<br />
    @Html.ActionLink("Back to Fares", "Index", "Fare")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EventName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EventDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EventName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EventDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EventTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EventTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EventTypeID })
        </td>
    </tr>
}

</table>
