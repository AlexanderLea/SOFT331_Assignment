@model IEnumerable<SOFT331_Assignment.Models.FareType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FareTypeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FareTypeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FaretypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FaretypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FaretypeID })
        </td>
    </tr>
}

</table>
